# 2-GPU extended suite — use case, intent, what is checked, status, reason

CUDA-only cases (`mg_deepcompile`, `mg_moe_experts_deepgemm`) are excluded: they are
invalid configurations on this platform, not test results.

`SKIP*` = failed before its dependency gate was added; now reports SKIP.

| # | Use case | What is being tested | What the case actually checks | Status | If not PASS — why |
|---|---|---|---|---|---|
| 1 | `mg_no_std_norm` | group_norm advantage WITHOUT std-dev normalization | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 2 | `mg_gamma_reinforce` | Discount factor γ=0.99 with the `reinforce` estimator — gamma is force-reset to 1.0 for every other estimator, so only reinforce can test it. | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 3 | `mg_lambda_gae` | GAE λ=0.95, which requires a critic, so a critic role is added and colocated. | Same as RL, plus **colocate_all** and vLLM+DeepSpeed sleep/wake each step so extra roles fit on 2 XPUs. Gloo transfer still verified per step. | **PASS** |  |
| 4 | `mg_gspo` | GSPO policy loss (`actor.policy_loss_type gspo`) instead of the default PPO clipped surrogate. | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 5 | `mg_dual_clip` | Dual-clip PPO (lower bound on negative-advantage tokens) | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 6 | `mg_value_clip` | Critic value clipping (non-default) | Same as RL, plus **colocate_all** and vLLM+DeepSpeed sleep/wake each step so extra roles fit on 2 XPUs. Gloo transfer still verified per step. | **PASS** |  |
| 7 | `mg_kl_unbiased` | Unbiased KL gradient on the KL-as-loss path | Same as RL, plus **colocate_all** and vLLM+DeepSpeed sleep/wake each step so extra roles fit on 2 XPUs. Gloo transfer still verified per step. | **PASS** |  |
| 8 | `mg_kl_adaptive` | Adaptive KL controller (target + horizon) | Same as RL, plus **colocate_all** and vLLM+DeepSpeed sleep/wake each step so extra roles fit on 2 XPUs. Gloo transfer still verified per step. | **PASS** |  |
| 9 | `mg_is_tv_gating` | IS correction gated by total-variation divergence | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 10 | `mg_reward_clip_range` | Reward clipping to a narrow range | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 11 | `mg_reward_model` | Reward MODEL as a 4th resident model on 2 XPUs | Same as RL, plus **colocate_all** and vLLM+DeepSpeed sleep/wake each step so extra roles fit on 2 XPUs. Gloo transfer still verified per step. | **PASS** |  |
| 12 | `mg_reward_offload` | Reward-model CPU offload | Same as RL, plus **colocate_all** and vLLM+DeepSpeed sleep/wake each step so extra roles fit on 2 XPUs. Gloo transfer still verified per step. | **PASS** |  |
| 13 | `mg_colocate_critic_reward` | colocate_critic_reward — critic+reward share a device | Same as RL, plus **colocate_all** and vLLM+DeepSpeed sleep/wake each step so extra roles fit on 2 XPUs. Gloo transfer still verified per step. | **PASS** |  |
| 14 | `mg_ref_offload` | Reference-model CPU offload | Same as RL, plus **colocate_all** and vLLM+DeepSpeed sleep/wake each step so extra roles fit on 2 XPUs. Gloo transfer still verified per step. | **PASS** |  |
| 15 | `mg_prefix_caching` | vLLM prefix caching across samples of a prompt | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 16 | `mg_sync_with_ray` | Weight sync via Ray's collective API (`vllm.sync_with_ray`) instead of the torch process group. | Same as RL, plus **colocate_all** and vLLM+DeepSpeed sleep/wake each step so extra roles fit on 2 XPUs. Gloo transfer still verified per step. | **FAIL** | Two configurations, two errors: default placement → `ValueError: the new group's world size should be less or equal to the world size set by init_process_group`; with colocate_all → `RuntimeError: The collective APIs shall be only used inside a Ray actor or task`. Ray's collective group must be created inside a Ray actor and is not on this path. Looks unmaintained upstream. |
| 17 | `mg_sampling_params` | Non-default rollout temperature / top_p | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 18 | `mg_overlap_comm` | overlap_comm explicitly ON at ZeRO-2 | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 19 | `mg_zpg` | ZeRO++ hierarchical partitioning (zpg=2) at stage 3 | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 20 | `mg_grad_accum_dtype` | Gradient-accumulation dtype forced to fp32 | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 21 | `mg_ppo_4bit` | 4-bit quantised actor inside the full RL loop, i.e. quantised weights must also be broadcast to the vLLM engine. | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. Here the transfer is exactly what breaks. | **FAIL** | bitsandbytes packs 4-bit weights as **uint8**; `vllm_worker_wrap.py:41` asserts every param matches the model dtype (`bf16`). The assert fires inside a Ray remote call on one side of the gloo collective, so the engine never joins and the actor blocks forever → **TIMEOUT**, only 2 of ~290 params synced. There is no dequantise-before-broadcast step. `mg_sft_4bit` PASSES, proving quantisation itself is fine. Not XPU-specific. |
| 22 | `mg_sft_4bit` | 4-bit (QLoRA) quantised SFT via bitsandbytes, deliberately WITHOUT vLLM so quantisation is tested in isolation from the rollout path. | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 23 | `mg_liger` | Liger fused kernels | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **SKIP** |  |
| 24 | `mg_flash_attn2` | `ds.attn_implementation flash_attention_2` on the actor. | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 25 | `mg_ds_autotp2` | DeepSpeed AutoTP: split the actor's tensors across both Arc B70s (`ds.tensor_parallel_size 2`), with 2 vLLM engines so the colocate_all device-count assert is satisfied. Model: Qwen2.5-0.5B. | Actor **sharded across BOTH XPUs** (real 2-rank DeepSpeed) + 2 vLLM engines. Checks cross-rank collectives AND gloo weight transfer. | **PASS** |  |
| 26 | `mg_ring_attn2` | Ring/sequence attention across 2 ranks (`ds.ring_attn_size 2`): shard the sequence dimension over both XPUs. Model: Qwen2.5-0.5B. | Actor **sharded across BOTH XPUs** (real 2-rank DeepSpeed) + 2 vLLM engines. Checks cross-rank collectives AND gloo weight transfer. | **SKIP*** | `ModuleNotFoundError: No module named 'ring_flash_attn'` — a missing OPTIONAL package, not a product defect. Now gated on the import so it reports SKIP; this FAIL predates that gate. |
| 27 | `mg_moe_sft` | SFT on a genuine Mixture-of-Experts model (granite-3.1-1b-a400m-instruct, 32 experts, top-8 routing) to prove the MoE code path runs at all. | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 28 | `mg_moe_aux_loss` | MoE router load-balancing auxiliary loss (`model.aux_loss_coef`). Meaningless on a dense model, so it needs a real router. | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **FAIL** | transformers' GraniteMoE ignores `output_router_logits=True`: `router_logits` comes back None and `aux_loss` is a literal `int 0`, so transformers' OWN code dies at `modeling_granitemoe.py:644` (`aux_loss.to(...)` → `'int' object has no attribute 'to'`). VERIFIED that Qwen2Moe and Mixtral both return a Tensor → **a transformers GraniteMoE bug, not OpenRLHF's**. Case re-pointed at a tiny Qwen2Moe; not yet re-run. |
| 29 | `mg_moe_experts_eager` | MoE expert compute strategy `eager` — the reference per-expert loop. | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 30 | `mg_moe_experts_batched_mm` | MoE expert compute strategy `batched_mm` — gathers one expert weight matrix per (token × top_k) pair. | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **FAIL** | `torch.OutOfMemoryError` twice — 4.81 GiB at `down_proj[expert_ids]`, then 3.39 GiB at `gate_up_proj[expert_ids]` after shrinking. `batched_mm` materialises one expert weight matrix per (token × top_k) pair, so with 32 experts/top-8/hidden-1024 the peak is inherent, not a batch-size choice. It had already completed 4/64 steps, so the kernel works. Re-pointed at a tiny 4-expert MoE; not yet re-run. |
| 31 | `mg_moe_experts_grouped_mm` | MoE expert compute strategy `grouped_mm` — grouped GEMM over experts. | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 32 | `mg_moe_rm` | Reward-model training on a real MoE, so the value head sits on top of a routed backbone. | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 33 | `mg_moe_grpo` | MoE RL rollout weight sync (expected FAIL: GraniteMoE name divergence) | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **NOT RUN** | Commented out by your explicit decision to park MoE RL. Two attempts, two different failures, zero steps: GraniteMoE dies on a runtime-vs-checkpoint parameter-name divergence, tiny Qwen2Moe clears the names then hits a shape mismatch that cannot be separated from a toy-checkpoint artefact. |
| 34 | `mg_grad_ckpt_reentrant` | Reentrant gradient checkpointing (RL actor) | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 35 | `mg_sup_grad_ckpt_reentrant` | Reentrant gradient checkpointing (SFT) | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 36 | `mg_muon_ppo` | Muon optimizer on actor AND critic | Same as RL, plus **colocate_all** and vLLM+DeepSpeed sleep/wake each step so extra roles fit on 2 XPUs. Gloo transfer still verified per step. | **PASS** |  |
| 37 | `mg_muon_sft` | Muon optimizer on SFT | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 38 | `mg_lora_variants` | LoRA with non-default alpha / dropout / target_modules | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 39 | `mg_ckpt_disable_ds` | HF-only checkpointing (skip the DeepSpeed ckpt) | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 40 | `mg_ckpt_best_metric` | Best-checkpoint selection by metric + rotation | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 41 | `mg_critic_extras` | Critic freezing + save value network | Same as RL, plus **colocate_all** and vLLM+DeepSpeed sleep/wake each step so extra roles fit on 2 XPUs. Gloo transfer still verified per step. | **PASS** |  |
| 42 | `mg_ppo_eval` | In-training evaluation: eval dataset, cadence, samples-per-prompt, temperature and split — the whole eval path, which no other 2-GPU suite enables. | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 43 | `mg_sft_eval` | Eval on the supervised path | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 44 | `mg_sft_zero3` | SFT with `ds.zero_stage 3`. NOTE: single process here, so stage 3 partitioning has no effect — this checks the stage-3 code path runs, not that it shards. | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 45 | `mg_sft_pretrain_mode` | SFT pretrain mode (plain LM loss, flat text column) | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 46 | `mg_rm_loss_type_fp32` | RM LogExp loss + fp32 loss computation | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 47 | `mg_rm_packing` | RM sample packing at ZeRO-3 | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 48 | `mg_dpo_nll` | DPO with an auxiliary NLL regularizer | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 49 | `mg_dpo_lora` | DPO + LoRA | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 50 | `mg_dpo_packing` | DPO sample packing | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **PASS** |  |
| 51 | `mg_data_chat_template` | Custom tokenizer chat template (model's own, passed explicitly) | **Single process, 1 XPU** (no launcher → world_size=1): forward, backward, optimizer step and data pipeline. No weight transfer; ZeRO-3 is a no-op here. PASS needs exit 0 + ≥1 loss line. | **FAIL** | Error changed after the fix, which is progress: my hand-written template gave `must contain at least one completion` (no assistant boundary for the loss mask); passing the model's own template gives `must contain at least one complete gradient-accumulation window` — the template is accepted and only the sample count is short. max_samples raised; not yet re-run. |
| 52 | `mg_prompt_probs` | Blend two prompt datasets with explicit sampling weights (`data.prompt_probs`). | Full RL loop on 2 XPUs: vLLM rollout, **gloo weight transfer actor→engine (sync PG world_size=2, ~290 params/step)**, advantage, optimizer step. PASS needs exit 0 + ≥1 `Global step`. | **PASS** |  |
| 53 | `mg_determinism` | Full determinism on the RL path | Runs the **same RL config twice** with `--train.seed 1234` and `--train.full_determinism_enable`, then compares the full aligned policy_loss sequence. PASS only if bit-identical. | **FAIL** | Diverges at step 1 with the comparison aligned (same step count, different values: runA `-0.012687 0.0 -0.020611 …` vs runB `0.0 -0.008001 0.018210 …`). It should reproduce — OpenRLHF sets `VLLM_ENABLE_V1_MULTIPROCESSING=0` (vLLM's documented reproducibility switch) and seeds each engine. `mg_determinism_sft` will show whether the source is the compute kernels or the rollout. |
| 54 | `mg_determinism_sft` | Full determinism on the SFT path | Runs the **same SFT config twice** with a fixed seed and compares the loss sequence. No vLLM and no sampling, so it separates non-deterministic compute from non-deterministic rollout. | **NOT RUN** | Created in the second fix pass, AFTER the last full suite run, so it has never executed. Its job is to tell us whether the RL determinism failure comes from the compute kernels or from rollout sampling. |
| 55 | `mg_universal_ckpt_load` | Load a converted universal checkpoint | Three stages: RL run writes a DeepSpeed ZeRO checkpoint → `ds_to_universal` converts it → a second RL run **resumes from the universal checkpoint**. Checks the output is loadable, not just that conversion exited 0. | **PASS** |  |
| 56 | `mg_lora_combiner` | Merge a trained LoRA adapter into the base model | Trains a LoRA adapter, then merges it into the base model with `openrlhf.cli.lora_combiner`. PASS requires **merged files on disk**, not just exit 0. | **PASS** |  |

## Totals

- **PASS: 46**
- **FAIL: 6**
- **NOT RUN: 2**
- **SKIP: 1**
- **SKIP*: 1**
- total (excluding invalid configs): 56
